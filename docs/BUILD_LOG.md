# PARALLEL product build log

This file records failures, root causes, fixes, and fresh evidence so later work does not repeat a solved mistake. No credentials, crops, prompts, or provider responses are recorded.

## Provider boundary

| State | Failure or observation | Root cause | Fix / decision | Fresh evidence |
|---|---|---|---|---|
| Fixed | Exa adapter originally used `x-api-key` and a top-level `highlights` flag. | The implementation followed an older request shape. | Use `Authorization: Bearer`, exact teaching-domain `includeDomains`, and `contents: { highlights: true }`; retain response-side allowlisting. | Mock boundary tests pass; live status-only Exa request returned HTTP 200. |
| Fixed | First live end-to-end smoke recognized the pattern, then failed during compilation. | The live path asked OpenAI to generate and self-certify the entire twin; its strict compilation response was also the safety proof. | OpenAI is classifier-only. The engine canonicalizes the classification and a deterministic verified compiler owns the statement, equations, mappings, confidence, and answer-leak boundary. | Live smoke now reaches `complete`; malicious compiler regression is rejected. |
| Fixed | A model-generated `exaQuery` and other signature prose could contain OCR, PII, or prompt injection. | The privacy boundary trusted prompt compliance. | Replace every textual signature field and Exa query with one of six hardcoded canonical patterns before emit, search, or persistence. | Prompt-injection/PII tests prove private strings do not reach events or Exa. |
| Fixed | The image classifier was forced to choose one of six patterns even for unrelated content or a visible live assessment. | The strict output contract had no refusal disposition. | Add explicit `supported`, `unsupported`, and `active_assessment` dispositions; strip attempt context and course-pack text from the provider request; stop before Exa or compilation on either refusal. | Provider and engine tests cover both generic refusal paths and prove private attempt text never enters the OpenAI request. |
| Fixed | Dismissal hid late UI events but did not stop an in-flight image request. | The desktop generation lease owned an `AbortSignal`, but the signal stopped at the UI delivery gate. | Thread the signal through desktop/API → engine → OpenAI and Exa, and silently end aborted streams. | Cancellation test aborts recognition mid-request with no late error, evidence query, or compilation; focused provider/API tests and workspace typecheck pass. |
| Fixed | Explicit demo mode still attempted live recognition when the launching shell contained provider credentials, and the demo child inherited those credentials and its live model override. | Provider selection checked for a live key before giving demo mode full control; the launcher copied the entire parent environment. | Make `PARALLEL_DEMO_MODE=1` an absolute offline branch and remove live credentials/model overrides from the Electron child environment. | A real launcher-process regression and an engine-level no-network regression both fail on the old behavior and pass on the fix. |
| Fixed | A temporary Exa failure terminated a recognized, structurally valid problem before deterministic compilation. | Evidence retrieval lived inside the same mandatory failure boundary as recognition and verified compilation. | Treat teaching evidence as optional enrichment: preserve aborts, discard all other provider diagnostics, and continue with an empty source set. | Engine regression reaches `complete` with zero sources and proves a private provider diagnostic is absent from every event. |
| Decision | Default Luna recognition latency was unstable: samples were 35.419 s, 1.940 s, and 19.223 s to recognition. | Provider/model latency variance, not local compilation or Exa (completion followed recognition by about 0.4–0.7 s). | Use Terra for recognition by default; keep the environment override. | Terra recognition samples were 1.569 s, 1.144 s, and 1.190 s; median 1.190 s. Full mapped completion samples were 2.031 s, 1.557 s, and 1.575 s; median 1.575 s. |

## Electron runtime

| State | Failure or observation | Root cause | Fix / decision | Fresh evidence |
|---|---|---|---|---|
| Fixed | Electron initially threw `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX`. | The runtime resolved workspace TypeScript instead of a bundled main process. | Bundle main/preload with esbuild into ignored generated output. | Actual Electron main-process probe passes. |
| Fixed | `better-sqlite3` could load in Node tests but fail in Electron. | Node and Electron use different native ABIs. | Build an ignored Electron-specific binding, pass it explicitly to both local stores, and restore the Node binding after build. | Runtime probe opens both SQLite stores and reports `sqlite_native_load=passed`. |
| Fixed | The native probe could report success without proving Electron or either SQLite store became ready. | It slept before attaching an exit listener, ignored exit status, and treated the absence of a blacklist regex as success. | Attach process lifecycle listeners immediately; reject early, nonzero, and timed-out exits; accept one exact sentinel emitted only after both stores execute a live query. | Controlled child-process tests cover exact readiness, clean early exit, nonzero exit, silence, and lookalike output; real store tests prove readiness hits an open database; the rebuilt app passes the exact-sentinel native probe. |
| Fixed | Generated `dist-electron` output was tracked. | Initial build output was committed before the ignore rule. | Remove generated binaries and ignore `dist-electron/` and the native binding. | Source tree remains clean after build. |
| Pending user acceptance | Computer Use could launch Electron but could not inspect or drive it. PARALLEL also lacked Screen Recording permission. | macOS Accessibility/Screen Recording grants were unavailable in this session. | User explicitly chose to skip Computer Use and perform the final hands-on overlay check. Automated native launch remains mandatory. | No claim of a Computer Use interaction test is made. |
| Fixed | `Option+Space` showed a blocking Screen Recording warning when access was not granted. | The product treated full-screen lasso as the only capture path, making a broad macOS permission feel mandatory. | Keep lasso for granted users; otherwise open a compact paste/drop/choose import view. Bind the optional Settings action to the active import window’s exact trusted document, reuse bounded crop validation, and keep bytes only in the active generation lease. | 84/84 desktop tests pass, including import UX/file/geometry and hostile IPC URL cases; desktop typecheck and production build pass. |
| Fixed | Import initially closed as soon as the twin opened, so Map projected anchors onto whatever was behind it instead of the imported problem. | Imported images were assigned a synthetic display-centered rectangle and no visible original remained. | Lock the import window as an in-memory **Original screenshot** reference, submit the preview element’s real screen rectangle, and close it with the sidecar. | Import geometry/UI regressions and desktop typecheck pass; final hands-on mapping alignment remains a user acceptance item. |
| Fixed | Sidecar and mapping windows could disappear behind a full-screen browser or PDF. | Only capture windows opted into all macOS Spaces and full-screen visibility. | Apply the same all-workspaces/full-screen visibility policy to result and mapping windows. | Source inspection plus desktop typecheck pass; full-screen behavior remains in the hands-on checklist. |

## Product correctness

| State | Failure or observation | Root cause | Fix / decision | Fresh evidence |
|---|---|---|---|---|
| Fixed | Mapping highlighted one rectangle around the whole lasso, and early verified twins still carried extra edges that had no recognized screen region. | The contract initially carried anchor names but no geometry; later compilation did not require an exact region ↔ edge join. | Add bounded normalized regions and step links, project into display coordinates, and compile only when every canonical region has exactly one verified mapping edge with real step IDs. | Contract, compiler exact-join, projection, and Sidecar pin/hover tests pass across all six patterns. |
| Fixed on branch | Several “worked” Statics twins omitted angles, magnitudes, geometry, or solved numeric results. | Early templates demonstrated structure but were not closed numerical problems. | Fully specify and solve all six templates with units and quantity provenance. | Seed sweep, hand-checked literals, determinism, no-leak, typecheck, and production build pass on `fix/math-templates`. |
| Fixed | Electron IPC trusted arbitrary renderer payloads; dismissal did not cancel late generation; `Another` was inert. | Prototype wiring lacked a hardened privileged boundary and lifecycle token. | Validate sender identity, document URL, payload shape, and active anchor IDs; reconstruct mapping geometry in main; invalidate generations; make regeneration reuse only the private in-memory crop. | Merged branch passed 99/99 tests, all workspace typechecks, production build, and native Electron/SQLite probe. |
| Fixed | The no-key provider treated every crop as the bundled moment fixture. | Demo mode was selected implicitly whenever an API key was absent. | Require explicit `PARALLEL_DEMO_MODE=1`, set only by the bundled demo command; fail closed for arbitrary no-key captures everywhere else. | Engine and end-to-end tests cover explicit fixed-fixture completion and non-demo refusal; the privacy scan remains zero across all counters. |
| Fixed | The 100-case evaluator failed immediately after exact mapping joins were enforced. | Its synthetic signatures still used the obsolete `problem-feature` placeholder anchor. | Generate the correct canonical anchor set for each gold pattern. | All 100 cases again report 100% schema validity and structural fidelity, with zero confident-wrong and answer-leak cases. |
| Fixed | `N` re-ran recognition with the same deterministic seed, uploading the crop again and returning the same twin. | Regeneration reused the entire first-pass pipeline instead of the already-validated abstract structure. | Compile the next bounded variation directly from the canonical signature and allowlisted evidence; never call image recognition or Exa again. | Regression tests prove variation changes the twin while structure/evidence providers are never invoked; typecheck and production Electron build pass. |
| Fixed | The generation session retained the raw crop after a successful twin solely to gate regeneration. | `N` had moved to abstract regeneration, but the old crop-owned lease API remained. | Release the lease’s private input at `recognized` (and in every terminal path), start regeneration without pixels, and scrub the caller-owned request object when the engine takes ownership. | Generation lifecycle and engine privacy regressions prove abstract regeneration keeps a valid lease without screenshot bytes. |
| Fixed | Every canonical capture in one pattern could score as a 100% Personal Precedent match, yet the stored row could not reopen the prior twin. | Canonical safety fields erased within-pattern differences; persistence kept only display metadata. | Require confidence ≥0.92 plus an exact one-way quantized shape fingerprint, persist only the safe generated twin, validate its mapping anchors, and replay it before Exa/compilation. Legacy metadata-only rows fail closed. | SQLite migration/privacy, changed-geometry rejection, confidence gate, safe replay, and negative-feedback regressions pass. |
| Fixed | The sidecar silently discarded `sourceRefs` and confidence, so a student could not distinguish “no teaching sources available” from a sourced twin. | Completion UI rendered only the statement, steps, and mapping controls. | Add a collapsed Sources & confidence shelf with structural confidence and explicit source availability; expansion renders only title and hostname, never a clickable or raw URL. | Component regressions cover the sourced and unavailable states, collapsed disclosure, safe domain-only rendering, and absence of links. |
| Fixed | The final full suite exposed one stale SidecarSession expectation after Personal Precedent stopped claiming a percentage. | The integration test still asserted the retired “Same shape” presentation copy. | Assert the truthful verified-local-match and prior-twin-reopened states at both component and session boundaries. | Focused component and session regressions cover both rendering boundaries. |
| Fixed | A calculus screenshot received the bundled “moment about point” Statics result. | The Electron fixed-fixture provider ignored pixels, while the capture UI still let an arbitrary screenshot enter that provider path. The UI then described a canned result as a completed twin. | Pivot the product surface to a web workspace and make the trust boundary structural: `/api/twins` is live-only and fails closed without recognition; `/api/demo` accepts one named bundled fixture and cannot accept a file. Never silently substitute demo output for an upload. | Forty web tests cover the split routes and UI truth states. In-browser verification uploaded the same calculus screenshot with no live key: the page showed the explicit `503` state, zero twins, and no demo banner. Selecting the demo afterward cleared the upload and rendered only the visibly labeled fixture. |
| Fixed | The first web mapping overlay used normalized coordinates against a stretched problem panel rather than the rendered image. | The image and highlight layer did not share a shrink-wrapped positioning context, so vertical coordinates drifted whenever the panel was taller than the image. | Wrap the image/SVG and mapping layer in one intrinsic-size `visual-frame`; project every normalized anchor inside that exact rectangle. | Browser geometry inspection proves the diagram and frame share the same bounds; the force-line highlight now starts at 18% and spans 50% of that exact image height. Both worked steps expose the expected one- and two-anchor mappings. |

## Build and dependency tooling

| State | Failure or observation | Root cause | Fix / decision | Fresh evidence |
|---|---|---|---|---|
| Fixed | pnpm initially tried to fetch private `@parallel/*` names from the public registry. | Internal manifests used bare `0.1.0` ranges, which did not prove workspace-only resolution. | Replace every internal range with `workspace:*`; remove the legacy lockfile and pin pnpm 11.9.0. | A frozen pnpm install resolves all seven workspaces locally and verifies 362 lock entries against supply-chain policy. |
| Fixed | pnpm refused native install scripts, and the first Next lock contained vulnerable transitive PostCSS and Sharp releases. | Build-script trust was implicit under the old installer, while the framework’s dependency graph lagged patched transitive releases. | Allow build scripts only for the four named native/build packages and override PostCSS/Sharp to patched exact releases. | Frozen install succeeds; `pnpm audit --audit-level high` reports no known vulnerabilities; preserved Electron tests remain 93/93 with typecheck passing. |
| Fixed | The first pnpm web build resolved a stale framework symlink left by the previous installer and then failed to load workspace TypeScript at runtime. | Generated dependency state did not match the new lockfile, and shared packages exported source-only paths that Turbopack interpreted differently from the test runner. | Move the stale generated directory aside, reinstall from the frozen pnpm lock, add explicit shared-package builds, and export compiled runtime entry points. | A clean frozen install, 40/40 web tests, web typecheck, and the Next production build all pass. |
| Fixed | Package-local web tests passed, but the repository-wide runner failed 10 checks. | Root Vitest did not inherit the web app's `@/` alias or DOM matcher setup. | Add one root Vitest configuration that owns the shared alias and matcher setup while preserving each test file's chosen environment. | The three focused suites pass 11/11; the repository runner now passes all 188 tests in one invocation. |
| Fixed | The first clean pnpm verification exposed undeclared workspace imports, React 19 JSX namespace errors, and a Node/Electron SQLite ABI mismatch after build. | A stale flattened root dependency tree had masked package ownership and made the native rebuilder save one physical binding while mutating another. | Move the generated legacy tree aside, reinstall solely from the frozen pnpm lock, declare every root/API dependency, resolve Electron and SQLite from `apps/desktop`, restore that exact Node binding after rebuild, and import React's JSX type explicitly. | Starting from the isolated pnpm graph: 188/188 tests and all workspace typechecks pass; production build reports `electron_native_build status=passed`; the immediately following privacy scan is all zeros; the Electron runtime reports `sqlite_native_load=passed`. A regression asserts native binding resolution stays inside the desktop workspace. |
| Fixed | The web UI and route initially accepted 8 MiB screenshots even though Vercel Functions reject request bodies above 4.5 MB. | The inherited desktop crop limit was copied into a serverless upload path without reserving multipart overhead. | Enforce 4 MiB in both browser and route, disclose the same limit in the UI and README, and keep the previous 8 MiB boundary only in the non-Vercel desktop path. | Focused tests prove a valid-signature 4 MiB + 1 byte crop is rejected before engine creation and the browser keeps analysis disabled for the same file. |
| Fixed | The first Vercel dry run selected Next correctly but planned to upload generated Electron bundles, a native SQLite binary, tests, and historical artifacts. | The monorepo project root was correct, but the generic ignore set did not express the much smaller production source boundary. | Add a root `.vercelignore` that retains the web app, shared packages, manifests, and lockfile while excluding desktop, generated output, tests, scripts, and collateral. | A second Vercel dry run still detects Next while reducing the upload from 219 files / 5,683,567 bytes to 62 files / 256,004 bytes. |
| Fixed | The first production build completed but warned that Vercel's injected `outputFileTracingRoot` and the explicit `turbopack.root` should match. | The app declared only the Turbopack side of the monorepo boundary, leaving Vercel to add the tracing side during build modification. | Declare both roots from the same canonical workspace path in `next.config.ts`. | Local production build passes; the replacement Vercel build completes without the mismatch warning and the stable alias points to that `READY` deployment. |
| Decision | Vercel could create and link the project but could not attach its GitHub integration. | The authenticated Vercel account has no GitHub Login Connection. | Preserve GitHub delivery through the existing repository and deploy the exact local branch through the authenticated Vercel CLI. | The stable production alias `https://web-sandy-seven-10.vercel.app` is `READY` on the latest reviewed runtime; GitHub PR #6 contains the deployed product commit and its later evidence-only commits. |
| Fixed | Code review found that standalone web typechecking did not explicitly generate ignored Next route types, and an archived Sites landing-page plan looked current. | The typecheck script relied on whatever `.next` state existed, while the historical plan lacked a superseded marker after the Vercel pivot. | Run `next typegen` before `tsc --noEmit` and add a prominent warning that routes contributors to `apps/web` and the root README. | With `.next` moved aside, web typecheck generates route types successfully; the full workspace typecheck and production web build pass. CodeRabbit reports success on the reviewed head. |
| Fixed | Review hardening found that an otherwise identical streamed step could be rejected when its JSON keys arrived in a different order, while malformed local file boundaries and native binding identity lacked direct regressions. | Event equality relied on serialization order; existing tests covered the size ceiling and package root but not every client-side file rejection or the exact native binary. | Compare the three worked-step fields directly; add non-image and zero-byte preview guards; assert the exact real SQLite binding selected from the desktop workspace. | The new test fails on serialization-based equality and passes after the fix; the pnpm-only repository suite passes 191/191, all workspace typechecks pass, and the production Next build succeeds. |

## Verification snapshots

- Initial finished prototype: 36/36 automated tests, all workspaces typechecked, build passed, 100-case synthetic preflight passed, privacy counters zero, dependency audit zero, native Electron/SQLite probe passed.
- Canonical intelligence branch: 41/41 tests plus typecheck/build/evaluate/privacy passed.
- Numerically closed Statics branch: 44/44 tests plus typecheck/build passed.
- Mapping branch: 39/39 tests plus typecheck/build/evaluate/privacy passed.
- First integrated desktop/mapping build: 99/99 tests plus typecheck/build/native probe passed.
- Privacy-first capture branch: 125/125 tests, all workspace typechecks,
  production build, 100-case evaluator, privacy scan, and native probe passed
  before integration.
- Reliable demo/evidence branch: 111/111 tests, all workspace typechecks, production Electron/native build, 100/100 deterministic evaluation cases, and zero privacy counters passed.
- Native-probe truth branch: 139/139 tests, all workspace typechecks,
  production build, controlled negative lifecycle fixtures, and the real
  exact-sentinel Electron/SQLite probe passed.
- Web product checkpoint: 40/40 web tests, web typecheck, production Next build,
  desktop and mobile browser checks, correct fixed-demo disclosure, honest live
  `503`, zero console warnings, no framework overlay, and no mobile overflow.
- Clean pnpm repository checkpoint: frozen install succeeds, 188/188 tests pass,
  all six typed workspaces pass, both production builds pass, 100/100 synthetic
  cases have structural fidelity with zero answer leakage, all five privacy
  counters are zero, the Electron/SQLite runtime probe passes, and the
  high-severity dependency audit finds no known vulnerabilities.
- Hosted production checkpoint: `https://web-sandy-seven-10.vercel.app` is
  `READY`; the strict demo API returns `fixedDemo: true`,
  `analyzedUpload: false`, two streamed steps, and `complete`; the no-key live
  API returns the intentional `503`; desktop and 390 px browser checks have no
  console errors, framework overlays, horizontal overflow, or code overflow;
  normalized mapping geometry shares the exact rendered diagram bounds; Vercel
  runtime error and warning queries are empty.
- Review-hardened checkpoint: 191/191 tests pass under pnpm, all workspace
  typechecks regenerate and consume current Next route types, and the optimized
  web production build succeeds.
